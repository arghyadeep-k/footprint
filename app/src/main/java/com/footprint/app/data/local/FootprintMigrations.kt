package com.footprint.app.data.local

import androidx.room.migration.Migration

object FootprintMigrations {
    // Add future version migrations here in ascending order, e.g. MIGRATION_1_2.
    // Keep this empty while DB version is 1.
    val ALL: Array<Migration> = emptyArray()
}
